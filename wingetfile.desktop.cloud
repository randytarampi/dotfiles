# wingetfile — Windows packages
# Format: one PackageIdentifier per line
# Comments start with #
# Categories mirror Brewfile categories where applicable

# --- Cloud / sync / AI ---
# Cross-ref: cask 'ollama-app'
Ollama.Ollama
# Cross-ref: cask 'opencode-desktop'
SST.OpenCodeDesktop
# Cross-ref: cask 'google-drive'
Google.GoogleDrive
# Cross-ref: cask 'cyberduck'
Cyberduck.Cyberduck
# Cross-ref: cask 'ngrok'
ngrok.ngrok
