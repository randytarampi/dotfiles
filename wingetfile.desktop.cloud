# wingetfile — Windows packages
# Format: one PackageIdentifier per line
# Comments start with #
# Categories mirror Brewfile categories where applicable

# --- Cloud / sync / AI ---
# No direct matches from current export.
# Add cloud/sync/AI packages here as the Windows inventory grows.
