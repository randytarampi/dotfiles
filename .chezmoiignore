# scripts/ are referenced by templates but not managed by chezmoi apply.
scripts/

# configs/ are referenced by run_once scripts, not applied to home directory.
configs/

# dot_mozart/ is a legacy template source; mozart.json is written by the script.
dot_mozart/

# Full iTerm2 preferences can include command history, hostnames, and secrets.
dot_com.googlecode.iterm2.plist

{{- if ne .chezmoi.os "darwin" }}
private_dot_sheepshaver_prefs
private_dot_basilisk_ii_prefs
{{- end }}

# Brewfile/wingetfile are consumed by run_onchange_08-install-packages.sh.tmpl
# from the chezmoi source directory — they should NOT be applied to ~/.
Brewfile*
wingetfile*
