{
  "name": "sentry",
  "type": "url",
  "url": "https://mcp.sentry.dev/mcp",
  "headers": {
    "Authorization": "Sentry-Bearer ${SENTRY_AUTH_TOKEN}"
  },
  "note": "Sentry MCP. Static token auth via SENTRY_AUTH_TOKEN (exported as SENTRY_ACCESS_TOKEN by shell). Remote server."
}
