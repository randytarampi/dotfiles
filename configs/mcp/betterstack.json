{
  "name": "betterstack",
  "type": "url",
  "url": "https://mcp.betterstack.com",
  "headers": {
    "Authorization": "Bearer ${BETTERSTACK_API_TOKEN}"
  },
  "note": "BetterStack MCP. Static API token auth via BETTERSTACK_API_TOKEN (OAuth also supported). Remote server."
}
