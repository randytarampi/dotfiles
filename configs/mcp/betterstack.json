{
  "name": "betterstack",
  "type": "command",
  "command": "npx",
  "args": ["-y", "mcp-remote", "https://mcp.betterstack.com", "--header", "Authorization: Bearer ${BETTERSTACK_API_TOKEN}"],
  "env": {
    "BETTERSTACK_API_TOKEN": "${BETTERSTACK_API_TOKEN}"
  }
}
